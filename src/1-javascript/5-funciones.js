function logName(name) {
  console.log(name);
}

// logName('pikachu');

const multiply = function (a, b) {
  return a * b;
};

console.log(multiply(4, 2));

const sum = (a, b) => {
  return a + b;
};

// console.log(sum(5, 10));
